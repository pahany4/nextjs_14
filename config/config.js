export const timer_value = 180;

export const toast_conf = {
  type: "success",
  autoClose: 3000,
  hideProgressBar: true,
  theme: "dark",
  position: "top-center"
}

export const slugOptions = {
  replacement: '_',
  remove: undefined,
  lower: true,
  strict: true,
  locale: 'ru',
  trim: true
}